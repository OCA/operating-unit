<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="report_saleorder_document_ou">
        <t t-call="report_qweb_operating_unit.external_layout_ou">
            <t t-set="doc" t-value="doc.with_context({'lang':doc.partner_id.lang})" />
            <div class="page">
                <div class="oe_structure"/>
                <div class="row">
                    <div class="col-xs-6">
                        <strong t-if="doc.partner_shipping_id == doc.partner_invoice_id">Invoicing and shipping address:</strong>
                        <strong t-if="doc.partner_shipping_id != doc.partner_invoice_id">Invoicing address:</strong>
                        <div t-field="doc.partner_invoice_id"
                            t-field-options='{"widget": "contact", "fields": ["address", "name", "phone", "fax"], "no_marker": true, "phone_icons": true}'/>
                        <p t-if="doc.partner_id.vat">VAT: <span t-field="doc.partner_id.vat"/></p>
                        <div t-if="doc.partner_shipping_id != doc.partner_invoice_id" class="mt8">
                            <strong>Shipping address:</strong>
                            <div t-field="doc.partner_shipping_id" 
                                t-field-options='{"widget": "contact", "fields": ["address", "name", "phone", "fax"], "no_marker": true, "phone_icons": true}'/>
                            <p t-if="doc.partner_id.vat">VAT: <span t-field="doc.partner_id.vat"/></p>
                        </div>
                    </div>
                    <div class="col-xs-5 col-xs-offset-1">
                        <strong>To:</strong>
                        <div t-field="doc.partner_id"
                             t-field-options='{"widget": "contact", "fields": ["address", "name", "phone", "fax"], "no_marker": true, "phone_icons": true}'/>
                    </div>
                </div>

                <h2>
                    <span t-if="doc.state not in ['draft','sent']">Order N </span>
                    <span t-if="doc.state in ['draft','sent']">Quotation N </span>
                    <span t-field="doc.name"/>
                </h2>

                <div class="row mt32 mb32" id="informations">
                    <div name="operating_unit" class="col-xs-3" t-if="doc.operating_unit_id">
                        <strong>Operating Unit:</strong>
                        <p t-field="doc.operating_unit_id"/>
                    </div>
                </div>

            </div>
        </t>
    </template>

    <template id="report_saleorder_qweb" inherit_id="sale.report_saleorder">
        <xpath expr="//t[@t-call='sale.report_saleorder_document']" position="attributes">
            <attribute name="t-call">sale_order_qweb_operating_unit.report_saleorder_document_ou</attribute>
        </xpath>
    </template>

</odoo>
