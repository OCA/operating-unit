This module allow a user to have Access all OUs' stock requests,
without having to add OUs in user setting.

Example use case, a shared team under an OU
but need to work on stock requests of all OUs.
